# Документация базы данных Волонтёрского центра

> **Описание:** Полная схема реляционной базы данных для управления волонтерами, мероприятиями, организациями и обратной связью. Реализована на Django ORM.

---

## Содержание

1. [Пользователи и Роли](#пользователи-и-роли)
   - [`users`](#users--пользователи)
   - [`managers`](#managers--менеджеры)
   - [`team_leaders`](#team_leaders--тим-лидеры)
2. [Организации и Организаторы](#организации-и-организаторы)
   - [`organizations`](#organizations--организации)
   - [`organizers`](#organizers--контактные-лица)
   - [`organizer_organization_link`](#organizer_organization_link--связь)
   - [`organizer_interaction_log`](#organizer_interaction_log--лог-взаимодействия-с-организатором)
3. [Справочники](#справочники)
   - [`locations`](#locations--локации)
   - [`event_categories`](#event_categories--категории)
   - [`seasons`](#seasons--сезоны)
   - [`thank_you_letter_templates`](#thank_you_letter_templates--шаблоны-благодарственных-писем)
   - [`dobro_ru_shared_links`](#dobro_ru_shared_links--общие-ссылки-добро-ру)
   - [`const`](#const--константы)
4. [Мероприятия](#мероприятия)
   - [`events`](#events--мероприятия)
   - [`event_category_map`](#event_category_map--связь-категорий)
   - [`event_days`](#event_days--дни-мероприятия)
   - [`event_staff`](#event_staff--персонал)
5. [Заявки и Участие](#заявки-и-участие)
   - [`applications`](#applications--заявки)
   - [`participations`](#participations--подтвержденные-участия)
   - [`churn_history`](#churn_history--история-чарна-по-сезонам)
   - [`user_comments`](#user_comments--комментарии-к-волонтёрам)
6. [Обратная связь и Требования](#обратная-связь-и-требования)
   - [`event_feedbacks`](#event_feedbacks--отзывы)
   - [`event_requirements`](#event_requirements--требования)
   - [`event_requirement_map`](#event_requirement_map--связь-требований)
   - [`user_requirement_exceptions`](#user_requirement_exceptions--исключения)
7. [Бизнес-правила и логика](#бизнес-правила-и-логика)

---

## Пользователи и Роли

### `users` — Пользователи (Volunteers)

Центральная сущность системы. Наследуется от `AbstractBaseUser`. Основной вход — через Telegram ID; дополнительно поддерживается вход на Web Platform по `login`/`password_hash` (см. бизнес-правило 8).

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `system_username` | VARCHAR(50) | Системный логин | Генерируется системой |
| `telegram_id` | VARCHAR(50) | Числовой ID Telegram (уникальный) | Telegram Bot API при первом входе |
| `telegram_username` | VARCHAR(100) | Никнейм в Telegram (@name) | Telegram Bot API при взаимодействии |
| `VK_username` | VARCHAR(100) | VK для связи | Ввод пользователем в профиле |
| `first_name` | VARCHAR(100) | Имя | Telegram API / Форма профиля |
| `last_name` | VARCHAR(100) | Фамилия | Telegram API / Форма профиля |
| `email` | VARCHAR | Электронная почта | Ввод пользователя |
| `phone` | VARCHAR(20) | Номер телефона | Ввод пользователя |
| `birthday` | DATE | Дата рождения | Ввод пользователя |
| `about` | TEXT | Информация о себе | Ввод пользователя |
| `gender` | ENUM(`male`, `female`) | Пол (`male` — мужчина, `female` — женщина) | Ввод пользователя |
| `status` | VARCHAR(20) | Статус: `active`, `inactive`, `blacklisted` | Установка менеджером |
| `comment` | TEXT | Внутренний комментарий менеджера | Ввод менеджером |
| `created_at` | TIMESTAMP | Дата регистрации | Автоматически |
| `last_event_at` | TIMESTAMP | Дата последнего участия | Обновляется системой после меро |
| `total_event_count` | INT | Мероприятий за все время | Агрегат `COUNT(participations)` |
| `season_event_count` | INT | Мероприятий за текущий сезон | Агрегат с фильтром по сезону |
| `total_hours` | INT | Часов за все время | Агрегат `SUM(participations.hours)` где `status='attended'` (менеджер может редактировать вручную) |
| `season_hours` | INT | Часов за текущий сезон | Агрегат с фильтром по сезону (менеджер может редактировать вручную) |
| `churn_score` | FLOAT | Коэффициент недобросовестности волонтёра на текущий момент | Пересчитывается системой, см. бизнес-правила. По достижении порога (`const.churn_blacklist_threshold`) — в блэклист до следующего сезона |
| `registration_status` | VARCHAR(20) | Прогресс онбординга: `guest` (первое касание, анкета не заполнена, доступен только просмотр меро) / `registered` (регистрация завершена, полный функционал волонтёра). Не пересекается со `status` | Устанавливается ботом при `/start` (`guest`) и по завершении регистрации (`registered`) |
| `notify_new_events` | BOOLEAN | Тумблер подписки на анонсы новых мероприятий (default false). Управляется волонтёром через `🔔 Подписка` | Переключается пользователем |
| `notify_applications` | BOOLEAN | Тумблер уведомлений о поступающих заявках для тим-лидеров/менеджеров (default true). Управляется через настройки уведомлений | Переключается пользователем (ТЛ/менеджер) |
| `login` | VARCHAR(50) | Логин для входа на Web Platform (nullable до установки, unique) | Задаётся при первом касании с ботом |
| `password` | VARCHAR(255) | Хеш пароля для Web Platform — унаследовано от `AbstractBaseUser` (не заводим отдельное поле `password_hash`, чтобы не дублировать хранение и не терять стандартные Django-механизмы хеширования/`set_password`) | Устанавливается/сбрасывается через бот/Mini App |
| `web_credentials_set_at` | TIMESTAMP | Когда установлены веб-креды (nullable) | Автоматически при установке |

### `managers` — Менеджеры

История выдачи и отзыва ролей менеджеров.

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `user_id` | FK → users | Ссылка на профиль волонтера | Ключ |
| `assigned_at` | TIMESTAMP | Дата назначения | Автоматически |
| `revoked_at` | TIMESTAMP | Дата отзыва (NULL = активна) | При отзыве роли |
| `assigned_by` | FK → users | Кто назначил роль | ID текущего менеджера |
| `total_events` | INT | Мероприятий курировал за все время | Агрегат по событиям в роли manager |
| `season_events` | INT | Мероприятий за сезон | Агрегат с фильтром по сезону |

### `team_leaders` — Тим-лидеры

История выдачи и отзыва ролей тим-лидеров.

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `user_id` | FK → users | Ссылка на профиль волонтера | Ключ |
| `assigned_at` | TIMESTAMP | Дата назначения | Автоматически |
| `revoked_at` | TIMESTAMP | Дата отзыва (NULL = активна) | При отзыве роли |
| `assigned_by` | FK → users | Кто назначил роль | ID текущего менеджера |
| `total_events` | INT | Мероприятий курировал за все время | Агрегат по событиям в роли team_lead |
| `season_events` | INT | Мероприятий за сезон | Агрегат с фильтром по сезону |

---

## Организации и Организаторы

### `organizations` — Организации

Юридические лица или структурные подразделения.

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `name` | VARCHAR(255) | Название организации | Ввод менеджером |
| `description` | TEXT | Описание деятельности | Ввод менеджером |
| `created_at` | TIMESTAMP | Дата добавления | Автоматически |
| `events_count` | INT | Кол-во мероприятий с поддержкой | Агрегат |
| `volunteers_count` | INT | Уникальных волонтеров | Агрегат |
| `shifts_count` | INT | Кол-во закрытых смен | Агрегат |
| `internal_rating` | SMALLINT | Внутренняя оценка организации (видна менеджерам и тим-лидерам; ставят менеджеры, а тим-лидеры — только после своего мероприятия) | Ввод менеджером/ТЛ |
| `comment` | TEXT | Внутренние комментарии (пишут менеджеры и тим-лидеры) | Ввод менеджером/ТЛ |

### `organizers` — Контактные лица

Люди, ответственные за коммуникацию.

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `user_id` | FK → users | Связь с профилем (если есть) | Опционально |
| `first_name` | VARCHAR(100) | Имя | Ввод менеджером |
| `last_name` | VARCHAR(100) | Фамилия | Ввод менеджером |
| `phone` | VARCHAR(20) | Телефон | Ввод менеджером |
| `email` | VARCHAR | Email | Ввод менеджером |
| `telegram_username` | VARCHAR(100) | Telegram | Ввод менеджером |
| `VK_username` | VARCHAR(100) | VK | Ввод менеджером |
| `whatsapp` | VARCHAR(50) | WhatsApp | Ввод менеджером |
| `comments` | TEXT | Комментарии | Ввод менеджером |

### `organizer_organization_link` — Связь организатор-организация

Many-to-Many связь.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `organizer_id` | FK → organizers | Организатор |
| `organization_id` | FK → organizations | Организация |
| `created_at` | TIMESTAMP | Дата установления связи |

### `organizer_interaction_log` — Лог взаимодействия с организатором

История контактов менеджеров с организатором (звонки, переписки, встречи, договорённости).

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `organizer_id` | FK → organizers | Организатор |
| `author_id` | FK → users | Кто внёс запись (менеджер) |
| `contact_type` | VARCHAR(20) | Тип контакта: `call`, `message`, `meeting` |
| `note` | TEXT | Текст заметки |
| `created_at` | TIMESTAMP | Дата записи |

---

## Справочники

### `locations` — Локации

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `name` | VARCHAR(255) | Название локации (уникальное) |

### `event_categories` — Категории мероприятий

Справочник тематик. Новые категории добавляет **менеджер**. При создании мероприятия менеджер выбирает одну или несколько категорий из выпадающего списка (связь — через `event_category_map`).

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `name` | VARCHAR(100) | Название категории (уникальное) |

### `seasons` — Сезоны

Периоды проведения ПГАС.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `created_at` | TIMESTAMP | Дата создания |
| `start_season` | DATE | Начало сезона |
| `end_season` | DATE | Конец сезона |

### `dobro_ru_shared_links` — Общие ссылки Добро.ру

Одна ссылка на Добро.ру, через которую учитываются часы **сразу нескольких** мероприятий (обычно мелких: короткие конференции, разовая помощь на 5–6 человек), чтобы не заводить отдельную ссылку на каждое. Мероприятие ссылается на неё через `events.dobro_ru_shared_link_id` при `dobro_ru_link_kind='shared'`.

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `title` | VARCHAR(255) | Человекочитаемое название (например, «Общая — конференции, апрель 2025») | Ввод менеджером |
| `url` | URL | Ссылка на Добро.ру | Ввод менеджером |
| `link_type` | VARCHAR(20) | Тип объекта: `event`, `organization`, `project` | Ввод менеджером |
| `period_hint` | VARCHAR(100) | Подсказка периода/охвата (напр. месяц), для группировки в отчёте | Ввод менеджером |
| `is_active` | BOOLEAN | Активна ли ссылка для привязки новых меро (default true) | Ввод менеджером |
| `created_at` | TIMESTAMP | Дата создания | Автоматически |

### `thank_you_letter_templates` — Шаблоны благодарственных писем

Хранятся только шаблоны. Сами благодарственные письма **не хранятся** — при запросе (скачивание/открытие) генерируются на лету путём подстановки ФИО волонтёра в шаблон. Связь «мероприятие → шаблон» задаётся через `events.letter_id`.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `title` | VARCHAR(255) | Название шаблона |
| `description` | TEXT | Описание шаблона |
| `pattern_link` | TEXT | Ссылка на файл шаблона |

### `const` — Константы системы

Key-value справочник настраиваемых бизнес-параметров. Значения задаются при инициализации БД и редактируются менеджером в отдельной вкладке (вместе с управлением сезонами). Тип значения — `FLOAT`, чтобы покрыть и целые пороги, и дробные коэффициенты.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `name` | VARCHAR(100) | Название константы (уникальное, машинный ключ) |
| `value` | FLOAT | Значение |
| `description` | TEXT | Человекочитаемое описание (для вкладки менеджера) |
| `created_at` | TIMESTAMP | Дата создания |
| `updated_at` | TIMESTAMP | Дата последнего изменения |

#### Обязательный набор констант (seed при инициализации БД)

| `name` | Значение по умолчанию | Смысл |
|--------|----------------------|-------|
| `churn_no_show_penalty` | `1` | Прибавка к чарну за неявку без предупреждения (`absent_unexcused`) |
| `churn_activity_base` | `3` | Числитель в бонусе за активность: `churn_activity_base / e^(меро за последний месяц + 1)` вычитается из чарна |
| `churn_blacklist_threshold` | `5` | Порог `churn_score`, при достижении которого волонтёр уходит в блэклист до следующего сезона |
| `churn_carryover_min_seasons` | `2` | Если у волонтёра уже есть ≥ этого числа заполненных записей в `churn_history`, при закрытии сезона чарн не обнуляется полностью, а снимается только половина |
| `pgas_hours_threshold` | `100` | Количество часов **за сезон** (`season_hours`) для закрытия ПГАС → автоматическая выдача статуса «Звёздочка ⭐» |
| `recruit_close_ratio` | `1.2` | Множитель: набор `closed`, когда одобренных заявок ≥ `recruit_close_ratio × volunteer_needed` |
| `late_cancel_hours` | `24` | За сколько часов до меро отказ считается «поздним сливом» (для будущей калибровки; фиксируется как параметр) |

> Значение `pgas_hours_threshold` (100) — предположение; уточнить у заказчика фактический порог ПГАС.
> Значение `churn_blacklist_threshold` (5) — предположение; калибруется по данным первого сезона.

---

## Мероприятия

### `events` — Мероприятия

Основная сущность события.

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `title` | VARCHAR(255) | Название мероприятия | Ввод менеджером |
| `description` | TEXT | Описание | Ввод менеджером |
| `location_id` | FK → locations | Место проведения | Выбор из справочника |
| `chat_link` | URL | Ссылка на чат Telegram | Ввод менеджером |
| `post_in_chanel_link` | URL | Ссылка на пост в канале | Ввод менеджером |
| `dobro_ru_link_kind` | VARCHAR(20) | Тип учёта часов на Добро.ру: `none` (ссылки нет — меро игнорируется в отчёте простановки), `shared` (часы учитываются через общую ссылку на несколько меро — см. `dobro_ru_shared_link_id`), `unique` (у меро своя уникальная ссылка в `dobro_ru_link`) | Ввод менеджером |
| `dobro_ru_link` | URL | Уникальная ссылка на Добро.ру (заполняется только при `dobro_ru_link_kind='unique'`) | Ввод менеджером |
| `dobro_ru_shared_link_id` | FK → dobro_ru_shared_links | Общая ссылка (только при `dobro_ru_link_kind='shared'`) | Выбор менеджером |
| `dobro_ru_hours_logged` | BOOLEAN | Проставлены ли часы на Добро.ру по всему меро (агрегатный флаг: true, когда по всем attended-участникам проставлено; default false) | Авто/Ввод ТЛ/Менеджером |
| `dobro_ru_logged_by` | FK → users | Кто закрыл простановку по меро (последний) | При простановке |
| `event_status` | VARCHAR(20) | Статус: `upcoming`, `ongoing`, `finished` | Авто/Ручное обновление |
| `recruit_status` | VARCHAR(20) | Набор: `open`, `closed` | Авто/Ручное обновление |
| `organization_id` | FK → organizations | Организующая структура | Ключ |
| `organizer_id` | FK → organizers | Контактное лицо | Ключ |
| `created_at` | TIMESTAMP | Дата создания записи в БД | Автоматически |
| `published_at` | TIMESTAMP | Дата анонса/публикации мероприятия на платформе (для аналитики времени отклика) | При публикации |
| `post_published_at` | TIMESTAMP | Момент публикации поста в Telegram-канале (`post_in_chanel_link`), nullable. Отдельно от `published_at` — для графика тайминга заявок с двумя отсечками (пост / анонс) | При публикации поста |
| `people_helped` | INT | Кол-во получивших помощь (обязательно при закрытии) | Ввод ТЛ/Менеджером |
| `internal_comment` | TEXT | Внутренние заметки по организации меро | Ввод ТЛ/Менеджером |
| `letter_id` | FK → thank_you_letter_templates | Шаблон благодарственного письма | Опционально |

### `event_category_map` — Категории мероприятий

Many-to-Many связь событий и категорий. У каждого мероприятия есть принадлежность к одной или нескольким категориям; они выбираются менеджером из выпадающего списка на этапе создания мероприятия (см. `event_categories`).

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `event_id` | FK → events | Мероприятие |
| `category_id` | FK → event_categories | Категория |

### `event_days` — Дни мероприятия

Разбивка на даты и слоты.

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `event_id` | FK → events | Родительское мероприятие | Ключ |
| `day` | DATE | Календарная дата | Ввод менеджером |
| `start_time` | TIMESTAMP | Время начала | Ввод менеджером |
| `end_time` | TIMESTAMP | Время окончания | Ввод менеджером |
| `volunteer_needed` | INT | Нужное кол-во волонтеров | Ввод менеджером |
| `location_id` | FK → locations | Локация дня | Ввод менеджером |

### `event_staff` — Персонал мероприятия

Назначение менеджеров и ТЛ на события.

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `event_id` | FK → events | Мероприятие | Ключ |
| `user_id` | FK → users | Профиль волонтера | Выбор менеджером |
| `staff_role` | VARCHAR(20) | Роль: `manager`, `team_lead` | Выбор менеджером |
| `assigned_at` | TIMESTAMP | Дата назначения | Автоматически |

---

## Заявки и Участие

### `applications` — Заявки

Входящие запросы на участие.

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `event_id` | FK → events | Мероприятие | Ключ |
| `user_id` | FK → users | Профиль (если есть) | Опционально |
| `first_name` | VARCHAR(100) | Имя из формы | Ввод заявителя |
| `last_name` | VARCHAR(100) | Фамилия из формы | Ввод заявителя |
| `telegram_username` | VARCHAR(100) | TG никнейм | Ввод заявителя |
| `phone` | VARCHAR(20) | Телефон | Ввод заявителя |
| `functions` | VARCHAR(255) | Функция | Ввод заявителя |
| `shifts` | VARCHAR(255) | Смена | Ввод заявителя |
| `role_in_event` | VARCHAR(255) | Роль | Ввод заявителя |
| `user_status` | VARCHAR(100) | Статус обращающегося | Ввод заявителя |
| `submitted_at` | TIMESTAMP | Момент **подачи** заявки заявителем. При подаче через ТГ-бота = `created_at`. При загрузке через форму/Excel берётся из формы (может отличаться от `created_at`) | ТГ-бот / форма |
| `created_at` | TIMESTAMP | Момент **загрузки** заявки в нашу систему | Автоматически |
| `assigned_at` | TIMESTAMP | Дата обработки (approve/reject). Используется для метрики «время обработки» = `assigned_at − created_at` | При обработке менеджером |
| `status` | VARCHAR(20) | Статус: `new`, `approved`, `rejected`. `rejected` = «резерв»: при недоборе ранее отклонённые заявки могут быть приняты | Менеджер |
| `assigned_by` | FK → users | Кто обработал | ID менеджера |

### `participations` — Подтвержденные участия

Факт участия волонтера в конкретном дне мероприятия.

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `user_id` | FK → users | Волонтер | Ключ |
| `event_day_id` | FK → event_days | Конкретный день | Ключ |
| `status` | VARCHAR(30) | Статус: `attended`, `absent_excused`, `absent_unexcused` | Тим-лидер |
| `full_shift` | BOOLEAN | Отработал ли волонтёр полную смену (default false). Проставляется тим-лидером на экране явки вместе со статусом | Тим-лидер |
| `hours` | INT | Отработанные часы. Засчитываются в статистику только при `status='attended'`. Менеджер может редактировать значение вручную | Расчёт или ручной ввод |
| `dobro_ru_logged` | BOOLEAN | Проставлены ли часы этого волонтёра на Добро.ру (default false). Отмечается менеджером в отчёте простановки — построчно (по человеку) или пакетно (по ссылке) | Ввод менеджером |
| `dobro_ru_logged_at` | TIMESTAMP | Когда отмечено (nullable) | Автоматически при отметке |
| `dobro_ru_logged_by` | FK → users | Кто отметил простановку по этому волонтёру (nullable) | При отметке |
| `created_at` | TIMESTAMP | Дата подтверждения | Автоматически |

### `churn_history` — История чарна по сезонам

Снимок значения `users.churn_score` на момент завершения сезона. Формируется при переходе на новый сезон: для всех, у кого есть ненулевой чарн, добавляется запись, после чего значение в профиле обнуляется/уменьшается (см. бизнес-правила).

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `user_id` | FK → users | Волонтёр | Ключ |
| `season_id` | FK → seasons | Сезон, за который зафиксирован чарн | Ключ |
| `churn_score` | FLOAT | Значение чарна на момент закрытия сезона | Снимок из `users.churn_score` |
| `created_at` | TIMESTAMP | Дата фиксации (закрытие сезона) | Автоматически |

### `user_comments` — Комментарии к волонтёрам

Внутренние комментарии, которые менеджеры и тим-лидеры пишут о волонтёре. Вынесены в отдельную таблицу (а не поле `users.comment`), чтобы поддержать контекстную видимость для тим-лидера (см. правило 5–6 в `access-control.md`): нужны автор, дата и контекст. Волонтёр свои комментарии **никогда** не видит.

| Поле | Тип | Описание | Источник данных |
|------|-----|----------|-----------------|
| `id` | INT PK | Уникальный идентификатор | Автоинкремент БД |
| `target_user_id` | FK → users | О ком комментарий | Ключ |
| `author_id` | FK → users | Кто написал (менеджер или тим-лидер) | Ключ |
| `event_id` | FK → events | Контекст мероприятия (nullable — общий комментарий менеджера) | Опционально |
| `text` | TEXT | Текст комментария | Ввод автором |
| `created_at` | TIMESTAMP | Дата | Автоматически |

> Поле `users.comment` остаётся как «главная» внутренняя заметка менеджера (одно на пользователя, только менеджеры). Множественные контекстные комментарии — в `user_comments`.

---

## Обратная связь и Требования

### `event_feedbacks` — Отзывы

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `author_id` | FK → users | Автор отзыва |
| `event_id` | FK → events | Мероприятие |
| `rating` | SMALLINT | Оценка 0-10 |
| `comment` | TEXT | Текст отзыва |
| `created_at` | TIMESTAMP | Дата публикации |

### `event_requirements` — Требования

Справочник требований (например, "18+", "Английский B2").

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `title` | VARCHAR(255) | Название требования |
| `description` | TEXT | Описание |

### `event_requirement_map` — Связь требований с мероприятиями

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `event_id` | FK → events | Мероприятие |
| `requirement_id` | FK → event_requirements | Требование |

### `user_requirement_exceptions` — Исключения пользователей

Хранит пользователей, которые **не** соответствуют требованию, но допущены.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT PK | Уникальный идентификатор |
| `user_id` | FK → users | Волонтер |
| `requirement_id` | FK → event_requirements | Требование |
| `reason` | TEXT | Причина исключения |
| `created_at` | TIMESTAMP | Дата внесения |

---

## Бизнес-правила и логика

1.  **Кэширование статистики:** Поля `total_hours`, `season_hours`, `total_event_count`, `season_event_count` в таблице `users` являются агрегированными (учитываются только участия со `status='attended'`). Пересчитываются при изменении статуса в `participations`. Менеджер может редактировать значения `*_hours` вручную.

2.  **Историчность ролей:** Активной ролью считается запись в `managers` или `team_leaders`, где `revoked_at IS NULL`.

3.  **Обработка заявок:**
    *   Менеджер (или тим-лидер на своём меро) принимает (`approved`) или отклоняет (`rejected`) заявку.
    *   `rejected` = «резерв»: при недоборе ранее отклонённые заявки могут быть приняты. Отдельного статуса «резерв» нет — это те же `rejected`.
    *   **Дубли разрешены и не скрываются.** Один пользователь может подать несколько заявок на одно меро (изменил смены/данные, забыл, что уже подавал). Уникального ограничения на `(user_id, event_id)` нет. Менеджер видит все заявки; актуальной считается последняя, но количество заявок показывается точно.

4.  **Связь заявки и участий:** При `approved` заявке автоматически создаются записи `participations` на **все** дни мероприятия (`event_days`). Тим-лидер затем удаляет лишние дни (≈90% мероприятий — однодневные).

5.  **Матчинг заявки с профилем (импорт из формы/Excel, `user_id` пуст):** искать существующего пользователя последовательно — (1) по `telegram_username`, (2) по `phone`, (3) по ФИО. Если совпадений нет — считаем пользователя новым и создаём профиль по данным заявки. При создании нового профиля выполнять проверку на возможные опечатки в ключевых полях (telegram/телефон/ФИО), чтобы не плодить дубли из-за очепяток.

6.  **Требования:** Таблица `user_requirement_exceptions` работает по принципу «белого списка исключений». Если записи нет — волонтёр считается соответствующим требованию.

7.  **People Helped:** Поле `people_helped` в `events` обязательно для заполнения при закрытии смены/мероприятия.

7a. **Полная смена:** тим-лидер при простановке явки может отметить `participations.full_shift`, зафиксировав, что волонтёр отработал полную смену. Признак не влияет на часы (они считаются отдельно), а используется как отметка добросовестного участия.

7b. **Категории мероприятия:** каждое мероприятие относится к одной или нескольким категориям (`event_category_map`). Категории выбираются менеджером из выпадающего списка при создании мероприятия. Новые категории в справочник `event_categories` добавляет менеджер.

7c. **Тумблеры уведомлений:**
    *   `users.notify_new_events` — волонтёр включает/выключает анонсы новых мероприятий (`🔔 Подписка`). Механика: при публикации нового меро диспетчер рассылает анонс всем, у кого `notify_new_events = true`.
    *   `users.notify_applications` — тим-лидер/менеджер включает/выключает уведомления о поступающих заявках. При `false` карточка новой заявки этому пользователю не приходит (заявки по-прежнему видны в разделе меро/`/allapplications`).

7d. **Повторная заявка (изменение данных):** волонтёр всегда может подать заявку. Если у него уже есть активная заявка на это меро — система предупреждает об этом и уточняет, хочет ли он изменить данные, предлагая посмотреть текущую заявку или изменить её. При изменении создаётся **новая** запись `applications`, которая считается актуальной (дубли не скрываются, см. правило 3).

7e. **Учёт часов на Добро.ру (ручная простановка менеджером):**
    *   Тип учёта задаётся на меро в `events.dobro_ru_link_kind`:
        *   `none` — ссылки нет; меро **исключается** из отчёта простановки (часы на Добро.ру по нему не ведутся).
        *   `shared` — часть меро (обычно мелких) учитывается через одну **общую** ссылку `dobro_ru_shared_links`, чтобы не плодить ссылки; несколько меро ссылаются на одну запись.
        *   `unique` — у меро **своя** ссылка в `events.dobro_ru_link` (напр. крупный ДОД на 100 волонтёров).
    *   Простановка — **ручная** операция вне системы (на сайте Добро.ру); в нашей системе менеджер лишь **отмечает факт** простановки, построчно по волонтёрам (`participations.dobro_ru_logged`) или пакетно по ссылке.
    *   Отмечать можно только участия со `status='attended'` и `hours > 0`.
    *   Агрегатный флаг `events.dobro_ru_hours_logged` = true, когда по всем attended-участникам меро проставлено (`dobro_ru_logged`); используется для быстрого статуса меро.
    *   Отчёт простановки строится в двух разрезах: **по человеку** (кому и сколько ставить) и **по ссылке** (какие меро/люди идут в конкретную ссылку) — см. `reports.md` §8.

8.  **Аутентификация (два канала входа в один аккаунт):**
    *   Аккаунт **всегда** создаётся через Telegram; `telegram_id` — первичный источник идентичности. Регистрация через веб без Telegram невозможна.
    *   **TG Bot / Mini App:** вход по `telegram_id` через проверку `initData`, без пароля.
    *   **Web Platform:** вход по `login` + `password_hash`. Логин/пароль задаются при первом касании с ботом; хранятся в `users`.
    *   **Сброс/смена пароля** — только через ТГ-бота/Mini App (`/start` → «сбросить веб-пароль»); self-service сброса на сайте нет.

9.  **Churn (коэффициент недобросовестности):**
    *   `users.churn_score` (FLOAT) — текущее значение на данный момент.
    *   **Начисление:** `+churn_no_show_penalty` (по умолчанию `+1`) за каждое меро с `absent_unexcused` (неявка без предупреждения). Проставляется тим-лидером вручную.
    *   **Списание за активность:** `− churn_activity_base / e^(N + 1)`, где `N` — количество мероприятий, на которых волонтёр был за последний месяц.
    *   **Блэклист:** при `churn_score ≥ churn_blacklist_threshold` волонтёр переводится в блэклист до следующего сезона / учебного года.
    *   **Закрытие сезона:** для всех с ненулевым чарном создаётся запись в `churn_history` (снимок значения), затем в профиле: если у волонтёра уже есть ≥ `churn_carryover_min_seasons` записей в `churn_history` — снимается только половина чарна; иначе — обнуляется.

10. **Статус набора (`recruit_status`):** `closed`, когда количество `approved` заявок ≥ `recruit_close_ratio × SUM(event_days.volunteer_needed)` (по умолчанию 1.2×). Фактическая явка (`attended`) — отдельная метрика, на закрытие набора не влияет.

11. **Сезоны:**
    *   Сезоны **не пересекаются**; даты задаёт менеджер в отдельной вкладке.
    *   Если предыдущий сезон закончился, а следующий не задан — автоматически создаётся новый сезон без даты окончания («до бесконечности»), а менеджерам и тим-лидерам показывается плашка-уведомление о необходимости задать дату конца сезона.
    *   При наступлении нового сезона обнуляются `season_*` поля и выполняется процедура churn-carryover (правило 9).

12. **«Звёздочка ⭐» / закрытие ПГАС:** статус выдаётся автоматически, когда волонтёр набирает `pgas_hours_threshold` часов **за сезон** (считается по `season_hours`). Отдельной сущности достижений нет — это производный признак. Порог `pgas_hours_threshold` — предположение (100), уточнить фактическое значение у заказчика.

13. **Оценка мероприятия и организации:**
    *   Единственная оценка меро — средний рейтинг из `event_feedbacks.rating` (отдельного поля `event_mark` нет).
    *   Оценку организации (`organizations.internal_rating`) и комментарии (`organizations.comment`) ставят/пишут менеджеры (в любой момент) и тим-лидеры (только после проведённого ими мероприятия этой организации). Видно только менеджерам и тим-лидерам.

14. **Константы:** пороги и коэффициенты (churn, ПГАС, множитель набора и т.п.) хранятся в таблице `const`, а не в коде; задаются при инициализации БД и редактируются менеджером.